 * After scroll, don't redraw immediately, queue the job.  If user scroll again, just remove the job then queue again.
 * Indicate more or no more pages in contents/bookshelf.
 * How to switch back to text page after go to content page.
 * Add UI states.
